{{! cargo-about template for THIRD-PARTY-NOTICES.md — rendered with the
    config in about.toml. Plain markdown so PR diffs are human-readable. }}
# Third-Party Notices

This file lists the third-party crates distributed inside Aegis binaries and
the licenses they carry. It is generated by
[cargo-about](https://github.com/EmbarkStudios/cargo-about) from `Cargo.lock`
(do not edit by hand); regenerate it with the command documented in
`about.toml` and commit the result alongside the dependency change.

## Licenses

{{#each licenses}}
### {{name}}

{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} — {{crate.repository}}{{/if}}
{{/each}}

```
{{{text}}}
```

{{/each}}
