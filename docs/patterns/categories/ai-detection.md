# ai-detection patterns

Heuristics that flag likely AI-generated code

**22 patterns** in this category. Return to the
[pattern index](../README.md) for the other categories and scoring
reference.

## Summary

| Pattern | Severity | Confidence | Description |
|----------|----------|------------|-------------|
| [`ai-detection-empty-catch-block`](#ai-detection-empty-catch-block) | low | high | Empty catch block detected |
| [`ai-generated-comment`](#ai-generated-comment) | low | medium | AI-generated code comment detected |
| [`ai-generated-marker`](#ai-generated-marker) | low | medium | AI generation marker detected |
| [`ai-header-comment`](#ai-header-comment) | low | medium | AI-style header comment with permissions |
| [`ai-overexplanation`](#ai-overexplanation) | low | low | AI overexplanation pattern detected |
| [`ai-placeholder`](#ai-placeholder) | low | medium | AI placeholder text detected |
| [`ai-repetitive-structure`](#ai-repetitive-structure) | low | low | Highly repetitive code structure detected |
| [`ai-template-marker`](#ai-template-marker) | low | medium | AI template marker detected |
| [`chatgpt-conversation`](#chatgpt-conversation) | low | low | ChatGPT prompt pattern detected |
| [`console-log-debug`](#console-log-debug) | low | low | Console logging statements detected |
| [`function-comment-every`](#function-comment-every) | low | low | Every function has descriptive comments (AI style) |
| [`function-name-verbose`](#function-name-verbose) | low | low | Verbose function naming detected |
| [`generic-variable-names`](#generic-variable-names) | low | low | Generic variable names detected (common in AI code) |
| [`import-bulk`](#import-bulk) | low | medium | Bulk import detected (common in AI-generated code) |
| [`markdown-code-fence`](#markdown-code-fence) | low | low | Markdown code fence detected |
| [`openai-format`](#openai-format) | low | low | Common AI assistant phrasing detected |
| [`response-format-json`](#response-format-json) | low | medium | Generic JSON response format pattern |
| [`semicolon-everywhere`](#semicolon-everywhere) | low | low | Excessive semicolons in JavaScript/TypeScript |
| [`superfluous-type-annotation`](#superfluous-type-annotation) | low | low | Redundant type annotation (TypeScript) |
| [`todo-still-present`](#todo-still-present) | low | low | TODO/FIXME comment still present in code |
| [`try-catch-bulk`](#try-catch-bulk) | low | low | Verbose try-catch blocks detected |
| [`very-long-line`](#very-long-line) | low | low | Very long line detected (common in AI output) |

## Pattern details

### ai-detection-empty-catch-block

Empty catch block detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `high` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `error-handling` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)catch\s*\([^)]*\)\s*\{\s*\}
```

**Input that fires** (verified by the liveness test):

```text
catch (n) { }
```

### ai-generated-comment

AI-generated code comment detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `medium` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `comment` |

**Match pattern** (Rust `regex` syntax):

```regex
//\s*(This code was|Generated by|Created by)\s+(AI|ChatGPT|Claude|GPT)
```

**Input that fires** (verified by the liveness test):

```text
// This code was AI
```

### ai-generated-marker

AI generation marker detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `medium` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `metadata` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(generated\s+by|ai[-\s]generated|created\s+by\s+(chat)?gpt|claude|anthropic|gemini)
```

**Input that fires** (verified by the liveness test):

```text
generated by
```

### ai-header-comment

AI-style header comment with permissions

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `medium` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `header` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(?:^|\n)\s*\*\s*(?:Permission|License|Author|Copyright)[\s\S]*?(?:\n|$)
```

**Input that fires** (verified by the liveness test):

```text
 * Permission    S  SS S SSS

```

### ai-overexplanation

AI overexplanation pattern detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `language` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(let me explain|basically|essentially|simply\s+put|in\s+other\s+words)
```

**Input that fires** (verified by the liveness test):

```text
let me explain
```

### ai-placeholder

AI placeholder text detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `medium` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `placeholder` |

**Match pattern** (Rust `regex` syntax):

```regex
\[(INSERT|REPLACE|FILL|TODO|ADD)\s+(YOUR|YOUR_|THE)
```

**Input that fires** (verified by the liveness test):

```text
[INSERT YOUR
```

### ai-repetitive-structure

Highly repetitive code structure detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `pattern` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(\{[^}]{0,20}\}\s*;?\s*){10,}
```

**Input that fires** (verified by the liveness test; long
token-shaped runs are elided here — the exact input is compiled into
`crates/aegis-patterns/src/examples.rs`):

```text
{pMpbUccF…b7/.} ; {+.CQFKk7o=ZJPg9KkvSX} ; {NwE_dQt3cQK=Qw.A5tET} ; {zAX=w:_o6oGUA+pyfu:h} ; {/bDwb ZmszBZVA…Gc} ; {GwT DRTZmMH4A.tk:m+8} ; {6g_WQK=93jKYTGBm:kw9} ; {x X-qc=vXQa=MiCp8wbd} ; {WYs.V+UVAEyp/aYu7TpA} ; {gEajgkfRpK/-3GSfE m7} ; 
```

### ai-template-marker

AI template marker detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `medium` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `template` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)ai[_\s]template|boilerplate\s+generated
```

**Input that fires** (verified by the liveness test):

```text
ai_template
```

### chatgpt-conversation

ChatGPT prompt pattern detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `prompt` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(you are a|act as a|i want you to act as)
```

**Input that fires** (verified by the liveness test):

```text
you are a
```

### console-log-debug

Console logging statements detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `debugging` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)console\.(log|debug|info|warn)\s*\(
```

**Input that fires** (verified by the liveness test):

```text
console.log (
```

### function-comment-every

Every function has descriptive comments (AI style)

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `documentation` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(?:\/\/|\/\*|\#\#)\s*(brief|summary|description|overview|documentation)
```

**Input that fires** (verified by the liveness test):

```text
// brief
```

### function-name-verbose

Verbose function naming detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `naming` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(?:function|def)\s+[a-z][a-z0-9_]{24,}\s*\(
```

**Input that fires** (verified by the liveness test; long
token-shaped runs are elided here — the exact input is compiled into
`crates/aegis-patterns/src/examples.rs`):

```text
function qhspuefn…y5 (
```

### generic-variable-names

Generic variable names detected (common in AI code)

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | `.js`, `.ts`, `.jsx`, `.tsx`, `.mjs`, `.cjs` |
| Binary files | skipped |
| Tags | `ai`, `naming` |

**Match pattern** (Rust `regex` syntax):

```regex
\b(?:const|let|var)\s+(?:data|info|value|result|output|item|element)\d*\b
```

**Input that fires** (verified by the liveness test):

```text
const data697234
```

### import-bulk

Bulk import detected (common in AI-generated code)

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `medium` |
| Scope | `file content` |
| Applies to | `.js`, `.mjs`, `.cjs`, `.ts`, `.tsx`, `.jsx` |
| Binary files | skipped |
| Tags | `ai`, `import` |

**Match pattern** (Rust `regex` syntax):

```regex
(?m)^import\s+\*\s+as\s+[A-Za-z_$][\w$]*
```

**Input that fires** (verified by the liveness test; long
token-shaped runs are elided here — the exact input is compiled into
`crates/aegis-patterns/src/examples.rs`):

```text
import * as r2riQ3G0…Ab
```

### markdown-code-fence

Markdown code fence detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `markdown` |

**Match pattern** (Rust `regex` syntax):

````regex
```\w*\n[\s\S]*?```
````

**Input that fires** (verified by the liveness test):

````text
```e9EPdczWcwJ
S  SSS   S S```
````

### openai-format

Common AI assistant phrasing detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `language` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(here['']s|as an ai|i can['']t|sure,? here|absolutely!?)
```

**Input that fires** (verified by the liveness test):

```text
here's
```

### response-format-json

Generic JSON response format pattern

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `medium` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `api` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(?:return|response)\s*=\s*\{\s*[\"']?(?:success|result|status|data)[\"']?\s*:
```

**Input that fires** (verified by the liveness test):

```text
return = { "success" :
```

### semicolon-everywhere

Excessive semicolons in JavaScript/TypeScript

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `style` |

**Match pattern** (Rust `regex` syntax):

```regex
;{2,}
```

**Input that fires** (verified by the liveness test):

```text
;;
```

### superfluous-type-annotation

Redundant type annotation (TypeScript)

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `typescript` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(?:const|let|var)\s+\w+:\s*\w+\s*=\s*\w+\s*\(
```

**Input that fires** (verified by the liveness test):

```text
const S7xkKBKr: 5eAd = u9dY (
```

### todo-still-present

TODO/FIXME comment still present in code

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `todo` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)(TODO|FIXME|HACK|XXX):
```

**Input that fires** (verified by the liveness test):

```text
TODO:
```

### try-catch-bulk

Verbose try-catch blocks detected

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `error-handling` |

**Match pattern** (Rust `regex` syntax):

```regex
(?i)try\s*\{[\s\S]*?\}\s*catch\s*\([^)]*\)\s*\{[\s\S]*?\}
```

**Input that fires** (verified by the liveness test):

```text
try {S   } catch (v6B/kay4 vs.) { SS}
```

### very-long-line

Very long line detected (common in AI output)

| Field | Value |
|-------|-------|
| Severity | `low` |
| Confidence | `low` |
| Scope | `file content` |
| Applies to | every text file |
| Binary files | skipped |
| Tags | `ai`, `format` |

**Match pattern** (Rust `regex` syntax):

```regex
^.{500,}$
```

**Input that fires** (verified by the liveness test; long
token-shaped runs are elided here — the exact input is compiled into
`crates/aegis-patterns/src/examples.rs`):

```text
/cF_UB3uU@Y.ETanFa4LXoTB/-h_Hpvzv…5d.C24ed.Lsi6sCzeeqyR o84x4mqa4Q6/Np8p3YNy…bt@gV@9FR6HzZmzu@8b9BGT_8…D_@vT5.uoRnbpb3…rG 9YygxPKc…j8 tB@buYBYNoy…cq/j8JZ9QsEs/6Z3XdqHsuyZv/fDFZKg6u…vq -vs/5HR3oJbp…5T.w_ SNekL7/2DN4/BXkzKWh7ofBL/KsfwvUu4…bF@TtiG/s2FGpkDW…yB 2SzmrMwa.7ZmeZFnK…eg spUdtiZzsi@gKE9GmxJ…iN/yyAiVc.n_yf-_ZZ…vU
```
